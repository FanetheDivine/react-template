---
alwaysApply: true
---

# 代码与组件规范

## TypeScript

- `tsconfig.json` 已开启 `strict`、`noEmit`，补充类型信息而非依赖隐式推断。
- 避免 `any`；若无法规避须注明原因。优先使用 `unknown`、字面量或工具类型。
- **默认使用 `type`** 声明结构与工具类型，仅在需要声明合并/模块扩展时使用 `interface`。
- 禁止 `enum`，改用联合字面量或常量对象。
- 导出类型使用命名导出，局部类型就近声明。
- `@/types/index.d.ts` 暴露 `Style`、`isFunction`、`ActionType` 等全局辅助类型；新增通用类型统一集中在 `@/types`。`ActionType` 适合描述“type + payload” 的联合结构：定义 `type ActionMap = { add: number; remove: undefined; patch?: Partial<User> }` 之后，可用 `ActionType<ActionMap>` 推导出 `{ type: 'add'; value: number } | { type: 'remove' } | { type: 'patch'; value?: Partial<User> }`；在 Reducer、命令分发或复杂事件流中按 `action.type` 做 `switch` 即可获得严格的 `value` 类型推断。
- `@/types/ValueController/index.ts` 声明自定义模块 `value-controller`，提供 `ValueController` 及相关工具类型，用来精准描述 `{ value, onChange }` 控制器的必填/可选、参数形态及是否支持函数式更新。

## React 组件与页面

- 项目基于 React 18 + React Router 7 + Vite。`src/index.tsx` 通过 `createBrowserRouter` 读取 `~pages` 并与 `AntdProvider`、`SWRProvider`、`withSuspense`、`withErrorBoundary` 组合，全局只渲染一次。
- `src/pages` 采用基于文件的路由（见 `file-based-router.md`）。`page`、`layout`、`loading`、`error`、`404` 文件必须默认导出组件：
  - `layout` 接收并渲染 `children`，这是一个由错误捕获、Suspense 边界和 `<Outlet/>` 组成的 `ReactNode`。
  - `error` 为 `FC<FallbackProps>`，由 `react-error-boundary` 提供 `error/resetErrorBoundary`。
  - `loading` 输出 Suspense fallback。
  - 缺失的文件对应的包裹层会被跳过。
- 组件文件使用 PascalCase 命名；hooks 必须以 `use` 开头。Ant Design 组件和图标按需导入，避免整包引入。
- 优先通过 `@/components/DefaultErrorFallback`、`@/components/DefaultLoadingFallback` 或自定义 Fallback 配合 `withErrorBoundary`、`withSuspense` 处理异步和异常。
- 复用逻辑抽离为自定义 hooks 或工具函数，保持页面组件聚焦在数据组合与 UI。

## 数据与状态

- HTTP 请求通过 Axios 发起，useSWR 辅助（支持 Suspense）；若需要更复杂场景，可结合 `zustand`、`immer/use-immer`、`rxjs`、`ahooks` 等。
- 后端接口封装统一放在 `@/api/XX`（按模块组织，如 `@/api/user`、`@/api/product`），由 useSWR 二次封装。
- 组件需要受控/半受控行为时，搭配 `useSemiControlledValue` + `ValueController` 类型约束；同步外部状态使用 `useImmediateEffect`。
- 输入法相关逻辑使用 `useComposition` 处理合成事件。

## 导入与路径

1. React / React Router / React DOM 等核心依赖
2. 第三方库（antd、lodash-es、zustand 等）
3. 内部模块（`@/api`、`@/components`、`@/utils`、`@/hooks` 等）
4. 类型导入（使用 `import type`）
5. 相对路径

使用 `@/*` 别名替代 `../../`。文件、模块命名需与目录语义一致。

## 样式

- Tailwind CSS 4 在 `src/styles/tailwind.css` 统一引入，项目级别的 className 常量（如 `AbsoluteCenter`）由 `@/styles/index.ts` 暴露。
- 默认使用 Tailwind 工具类。多段或语义分组时借助 `@/utils` 中的 `cn`（`clsx + tailwind-merge`）。
- 对于难以用 Tailwind 表达的样式（含空格的属性值、复杂阴影、渐变、grid span 等），写入独立 CSS/模块化样式或少量 `style` 内联即可，不必强行拆分成若干工具类。
- 修改 Ant Design 主题优先通过 Provider Token 或 CSS，避免直接覆盖 `antd/dist/reset.css`。
- 项目不引入 Sass/Less；如需额外样式文件使用原生 CSS。

## 工具与库

- `@/utils` 汇总所有通用 helper：`cn`、`sleep`、`loadFile`、`isReactNode`、`withSuspense`、`withErrorBoundary`、自定义 RxJS 运算符等。优先调用这些封装，保持实现一致。
- `@/lib` 放置全局 Provider、路由转换器、404 页面等一次性初始化能力。业务组件一般不会直接引用这些代码。
- `@/components` 提供默认的 Loading/Error 组件，可直接复用或作为参考实现。

## Hooks

- 自定义 hooks 位于 `@/hooks` 并在 `index.tsx` 聚合导出（`useImmediateEffect`、`useSemiControlledValue`、`useComposition` 等）。
- Hooks 必须遵循 React 规则：只在函数顶层调用、依赖数组完整、避免条件调用。

## 代码质量

- 每次提交前运行 `pnpm prettier`（或使用编辑器格式化）与 `pnpm lint`，保证 ESLint 9 + Prettier 3 规则通过，尤其关注 `react-hooks/exhaustive-deps`。
- 复杂逻辑与公共 API 提供 JSDoc/注释，帮助后续维护。
- 提交前确保编译通过、类型检查无误。
